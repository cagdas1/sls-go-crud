service: serverless-go-crud

provider:
  name: aws
  region: us-west-1
  runtime: go1.x

package:
 exclude:
   - ./**
 include:
   - ./bin/**

functions:
  hello:
    handler: apis/create
    events:
      - http:
          path: /
          method: post